{% extends 'base.html.twig' %}

{% block title %}Edit Municipality{% endblock %}

{% block body %}
    <h1> <small> Metre à jour une Municipalité </small> </h1>
       
        {{ include('municipality/_form.html.twig', {'button_label': 'Metre à jour'}) }}
        
        <div class="container-fluid">
                <div class="pull-right">
                        <a class="btn btn-info" href="{{ path('app_municipality_index') }}" role="button"> Retour à la liste </a>  <br> <br>    
                </div>
                
                {{ include('municipality/_delete_form.html.twig') }}
        </div>

{% endblock %}
