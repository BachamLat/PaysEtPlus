{% extends 'base.html.twig' %}

{% block title %}Municipality{% endblock %}

{% block body %}
    <h1> <small> Municipalité </small> </h1>

    <table class="table table-striped table-hover">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ municipality.id }}</td>
            </tr> #}
            <tr>
                <th>Nom</th>
                <td>{{ municipality.name }}</td>
            </tr>

            <tr>  <td colspan="2"> </td>  </tr>

            <tr>
                <th>Longitude</th>
                <th>Latitude</th>
            </tr>
            <tr>
                <td>{{ municipality.longitude }}</td>
                <td>{{ municipality.latitude }}</td>
            </tr>
        </tbody>
    </table>

        <div class="container-fluid">
            <div class="row">
                <div class="pull-left">
                    <a class="btn btn-default" href="{{ path('app_municipality_edit', {'id': municipality.id}) }}" role="button"> Editer </a> 
                </div>

                <div class="pull-right">
                    <a class="btn btn-default" href="{{ path('app_municipality_index') }}" role="button"> Retour à la liste </a>         
                </div>

                {{ include('municipality/_delete_form.html.twig') }}
            </div>
        </div>

{% endblock %}
