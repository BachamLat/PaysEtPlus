<form method="post" action="{{ path('app_district_delete', {'id': district.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <div id="btnPosition">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ district.id) }}">
        <button class="btn btn-info"> Supprimer </button>
    </div>
</form>
