{% extends 'base.html.twig' %}

{% block title %}Edit Town{% endblock %}

{% block body %}
    <h1> <small> Metre à jour une Ville </small> </h1>

    {{ include('town/_form.html.twig', {'button_label': 'Metre à jour'}) }}
        <div class="container-fluid">
            <div class="pull-right">
                    <a class="btn btn-default" href="{{ path('app_town_index') }}" role="button"> Retour à la liste </a>  <br> <br>    
            </div>
                
            {{ include('town/_delete_form.html.twig') }}
        </div>
  
{% endblock %}
