{% extends 'base.html.twig' %}

{% block title %}Town{% endblock %}

{% block body %}
    <h1> <small> Ville </small> </h1>

    <table class="table table-striped table-hover">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ town.id }}</td>
            </tr> #}
            <tr>
                <th>Nom</th>
                <td>{{ town.name }}</td>
            </tr>

            
            {% if town.longitude == 0  %}
            {% else %}
            <tr>  <td colspan="2"> </td>  </tr>
            <tr>
                <th>Longitude</th>
                <th>Latitude</th>
            </tr>
            <tr>
                <td id="long">{{ town.longitude }}</td>
                <td id="lat">{{ town.latitude }}</td>
            </tr>
            {% endif %}

        </tbody>
    </table>

        <div class="container-fluid">
            
                <div class="pull-left">
                    <a class="btn btn-info" href="{{ path('app_town_edit', {'id': town.id}) }}" role="button"> Editer </a> 
                </div>

                <div class="pull-right">
                    <a class="btn btn-info" href="{{ path('app_town_index') }}" role="button"> Retour à la liste </a>         
                </div>

                {{ include('town/_delete_form.html.twig') }}
           
        </div>
        {% if town.longitude == 0  %}
        {% else %}
        <div class="map-example">
            <div class="row">
                <div class="col-lg">

                    <div id="map"> </div>
                   
                </div>
            </div>
        </div>
        {% endif %}

{% endblock %}
