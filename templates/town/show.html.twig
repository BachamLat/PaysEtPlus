{% extends 'base.html.twig' %}

{% block title %}Town{% endblock %}

{% block body %}
    <h1> <small> Ville </small> </h1>

    <table class="table table-striped table-hover">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ town.id }}</td>
            </tr> #}
            <tr>
                <th>Nom</th>
                <td>{{ town.name }}</td>
            </tr>

            <tr>  <td colspan="2"> </td>  </tr>

            <tr>
                <th>Longitude</th>
                <td>{{ town.longitude }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ town.latitude }}</td>
            </tr>
        </tbody>
    </table>

        <div class="container-fluid">
            <div class="row">
                <div class="pull-left">
                    <a class="btn btn-info" href="{{ path('app_town_edit', {'id': town.id}) }}" role="button"> Editer </a> 
                </div>

                <div class="pull-right">
                    <a class="btn btn-info" href="{{ path('app_town_index') }}" role="button"> Retour à la liste </a>         
                </div>

                {{ include('town/_delete_form.html.twig') }}
            </div>
        </div>

{% endblock %}
