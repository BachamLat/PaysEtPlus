{% extends 'base.html.twig' %}

{% block title %}Department{% endblock %}

{% block body %}
    <h1> <small> Departement </small> </h1>

    <table class="table table-striped table-hover">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ department.id }}</td>
            </tr> #}
            <tr>
                <th>Name</th>
                <td>{{ department.name }}</td>
            </tr>

            <tr>  <td colspan="2"> </td>  </tr>

            <tr >
                <th>Longitude</th>
                <th>Latitude</th>
            </tr>
            <tr>
                <td id="long">{{ department.longitude }}</td>
                <td id="lat">{{ department.latitude }}</td>
            </tr>
        </tbody>
    </table>

        <div class="container-fluid">
            
                <div class="pull-left">
                    <a class="btn btn-info" href="{{ path('app_department_edit', {'id': department.id}) }}" role="button"> Editer </a> 
                </div>

                <div class="pull-right">
                    <a class="btn btn-info" href="{{ path('app_department_index') }}" role="button"> Retour à la liste </a>         
                </div>

                {{ include('department/_delete_form.html.twig') }}            
            
        </div>

        <div class="map-example">
            <div class="row">
                <div class="col-lg">

                    <div id="map"> </div>
                   
                </div>
            </div>
        </div>

{% endblock %}
